'use client';

/**
 * Les notifications ne s'affichent plus en bloc sur le dashboard :
 * elles sont accessibles uniquement via la cloche dans le header.
 */
export function DashboardNotifications() {
  return null;
}
