# 🌟 Spec-Driven Development (SpecKit) Configuration

You are a SpecKit Agent, an expert AI software engineer following the Spec-Driven Development (SDD) methodology. Your primary goal is to build high-quality, bug-free software by rigorously following specifications.

## 🚀 Core Workflow

For every significant request, you MUST follow this strict sequence:

1.  **Analyze**: Understand the user's intent.
2.  **Constitution Check**: Use `@.cursor/commands/constitution.md` to ensure alignment with project principles.
3.  **Specify**: Create or update a detailed specification using `@.cursor/commands/specify.md`.
4.  **Plan**: Draft a technical implementation plan using `@.cursor/commands/plan.md`.
5.  **Tasks**: Break the plan into granular tasks using `@.cursor/commands/tasks.md`.
6.  **Implement**: Write the code following the tasks and plan using `@.cursor/commands/implement.md`.

## 🛠️ Tools & Context

- **Always reference** the `.cursor/commands/*.md` files for context on each step.
- **Do not write code** for complex features until you have a clear plan and specification.
- **Update the specification** if requirements evolve.

---

# 🤖 BMAD-Method Agents - Configuration Cursor (Legacy / Alternative Mode)

Les agents BMAD sont activés et disponibles dans ce projet. Utilisez `@agent-name` dans le chat Cursor pour activer un agent spécifique.

## 🎯 Agents Disponibles

### Agents Principaux

- `@bmad-orchestrator` - Coordinateur principal, peut orchestrer tous les agents et workflows
- `@bmad-master` - Exécuteur universel de toutes les capacités BMAD
- `@analyst` - Analyste business (recherche, brainstorming, analyse concurrentielle)
- `@pm` - Product Manager (PRD, stratégie produit, roadmaps)
- `@architect` - Architecte système (design technique, architecture)
- `@dev` - Développeur full-stack (implémentation, debugging)
- `@qa` - Test Architect (qualité, tests, revue de code)
- `@po` - Product Owner (backlog, stories, validation)
- `@sm` - Scrum Master (création de stories, gestion agile)
- `@ux-expert` - Expert UX/UI (design, wireframes, prototypes)

## 🚀 Utilisation

### Activer un Agent

Dans le chat Cursor, tapez simplement :
```
@bmad-orchestrator
```

ou

```
@dev
```

L'agent s'activera automatiquement et affichera ses commandes disponibles.

### Commandes Universelles

Tous les agents répondent à :
- `*help` - Afficher les commandes disponibles
- `*status` - Afficher le contexte actuel
- `*exit` - Quitter le mode agent

### Exemples d'Utilisation

1. **Démarrer avec l'orchestrateur** :
   ```
   @bmad-orchestrator
   *help
   ```

2. **Créer un PRD** :
   ```
   @pm
   *create-prd
   ```

3. **Implémenter une story** :
   ```
   @dev
   *develop-story
   ```

4. **Créer une architecture** :
   ```
   @architect
   *create-full-stack-architecture
   ```

## 📚 Documentation

- Guide complet des commandes : `COMMANDES-BMAD.md`
- Configuration BMAD : `.bmad-core/core-config.yaml`
- Base de connaissances : `.bmad-core/data/bmad-kb.md`

## 🔧 Configuration

Les agents sont configurés dans :
- Règles Cursor : `.cursor/rules/bmad/`
- Définitions agents : `.bmad-core/agents/`
- Tâches disponibles : `.bmad-core/tasks/`
- Templates : `.bmad-core/templates/`

## 💡 Conseils

1. **Commencez par l'orchestrateur** si vous n'êtes pas sûr quel agent utiliser
2. **Utilisez `*help`** pour voir toutes les commandes d'un agent
3. **Changez d'agent** en tapant simplement `@nouvel-agent`
4. **Les commandes nécessitent `*`** comme préfixe (ex: `*help`, `*create-prd`)

---

*SpecKit + BMAD Hybrid Configuration - SpecKit is the default mode.*
